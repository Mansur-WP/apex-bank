[project]
name = "apex-bank"
version = "0.1.0"
description = "A modern banking simulator built with Django and PostgreSQL"
requires-python = ">=3.11"
dependencies = [
    "dj-database-url>=3.1.2",
    "django>=5.2.14",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.2",
]
